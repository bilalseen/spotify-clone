import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: "100%",
    height: "auto",
    gap: 10,
    marginVertical: 20,
  },
  title: {
    fontSize: 22,
    fontWeight: "600",
    color: "#fff",
  },
});

export default styles;
